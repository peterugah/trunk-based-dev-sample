name: Deploy tag
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Print deployment start message
        run: |
          echo "DEMO DEPLOYMENT TRIGGERED"
          echo "Ref:       ${{ github.ref }}"
          echo "Ref name:  ${{ github.ref_name }}"
          echo "This is just a demo — no real deployment executed."

      - name: Checkout selected ref (tag/branch)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Simulate work
        run: |
          echo "Pretending to deploy ${{ github.ref_name }}..."
          sleep 2
          echo "Done!"
